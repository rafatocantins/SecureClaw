{
  "name": "@tessera/integration",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "if docker info > /dev/null 2>&1; then vitest run --reporter=verbose; else echo 'Docker not available, skipping integration tests'; fi"
  },
  "devDependencies": {
    "@types/ws": "^8.5.13",
    "vitest": "^3.0.0",
    "ws": "^8.18.0"
  }
}
